## 重要：REPL 循环重构 — 为 G1 Rich REPL 做前期准备
- 新增 `agent/repl.py`：ReplLoop（通用事件循环）+ CommandRegistry（/命令管理）+ StdioInputBackend + StdioOutputBackend
- 重构 `agent/cli.py`：内联 REPL while 循环替换为 ReplLoop 调用
- 新增 `test/test_repl.py`：13 个测试覆盖 CommandRegistry / ReplLoop

### 设计要点
- 当前 StdioInputBackend/StdioOutputBackend 基于 input()/print()
- G1 只需在新的 `agent/repl_prompt_toolkit.py` 中实现 prompt_toolkit 版 InputBackend，ReplLoop 不需要动
- 未来 TUI = 新的 InputBackend + OutputBackend 实现，ReplLoop 仍然不需要动
- CommandRegistry 支持 register() 动态注册 / 命令，内置 /exit + /help

## 重要：G3 — 配置系统 (chips config set/get/list)
- 新增 `config/store.py`：ConfigStore 封装 ~/.chips/config.yaml 的读写
- 新增 `config/cli.py`：handle_config 处理 config set/get/list 子命令
- 重构 `agent/cli.py`：argparse 添加 config 子命令；`_load_config()` 替换为 ConfigStore().apply_to_env()；_build_parser() 拆出独立函数
- 新增 `test/test_config.py`：15 个测试覆盖 store + cli

## 重要：G2 — Session 管理 (chips session list/show/search/delete)
- 新增 `session/cli.py`：handle_session 处理 session list/show/search/delete 子命令
- 修改 `agent/cli.py`：argparse 添加 session 子命令
- 修改 `session/db.py`：search() 加 LIKE 回退，解决 FTS5 不识别中文的问题
- 新增 `test/test_session_cli.py`：8 个测试
