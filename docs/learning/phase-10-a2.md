# Phase 10 A2 学习笔记

## 上下文压缩保护

超限时双阶段降级：

1. **压缩**：tool 内容 >2000 字符截断 + `...(truncated)`，保留结构
2. **删组**：assistant(with tool_calls) + 紧随 tool 为原子组，从中间逐组删（保护第 1 组 user 意图 + 最后 2 组近期上下文），每次删后重建分组，从高索引倒序 pop 防偏移
