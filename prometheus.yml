# chips Prometheus 配置
# 启动：docker compose up -d prometheus grafana
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "chips"
    static_configs:
      - targets: ["host.docker.internal:8648"]
        labels:
          service: "chips-agent"
